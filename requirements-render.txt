# FastAPI 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 파일 업로드 처리
python-multipart==0.0.6

# 환경 변수 관리
python-dotenv==1.0.0

# 데이터 검증
pydantic==2.9.0
pydantic-settings==2.6.0

# HTTP 클라이언트 (AI API 호출용)
httpx==0.25.2

# 파일 처리
Pillow==10.4.0
aiofiles==23.2.1

# OCR 라이브러리 (렌더 최적화)
# google-cloud-vision==3.10.2  # 렌더에서 메모리 부족으로 제외
pytesseract==0.3.10
# easyocr는 메모리 부족으로 제외

# 문서 처리
pypdf==4.0.1
docx2txt==0.8
chardet==5.2.0
olefile==0.46

# 테스트용
requests==2.31.0

# 개발용 도구
pytest==7.4.3
